<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.drowsydrivingdetection">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DrowsyDrivingDetection"
        tools:targetApi="31">

        <activity
            android:name=".ui.pages.ModelPage"
            android:exported="true">

        </activity>

        <activity
            android:name=".core.OpenCV"
            android:exported="true">

        </activity>

        <activity
            android:name=".ui.pages.HomeActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.pages.FaqActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".ui.pages.ProfileActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.pages.ModelActivity2"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.pages.SignInActivity"
            android:exported="true" />

        <activity
            android:name=".ui.pages.ProfileNonRegisteredActivity"
            android:exported="true" />

        <activity
            android:name=".ui.pages.RegistrationActivity"
            android:exported="true" />

        <activity android:name=".ui.pages.AlertsDashboard" />

        <activity
            android:name=".ui.pages.ForgotPasswordActivity"
            android:exported="true" />

        <activity
            android:name=".ui.pages.ResetPasswordActivity"
            android:exported="true" />

        <activity
            android:name=".ui.alerts.AlertActivity"
            android:exported="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>
